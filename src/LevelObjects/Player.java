package LevelObjects;

import javafx.scene.paint.ImagePattern;

public class Player extends Entity {

    /**
     * Creates a new Player with given Height, Width, and skin
     *
     * @param height The height of the new Player
     * @param width The width of the new Player
     * @param skin The skin of the new Player
     */
    public Player(int height, int width, ImagePattern skin) {
        super(height, width, skin);
        //this.setLeft(null);
        //this.setRight(null);
        //this.setJump(null);
        //this.setDown(null);
    }

}
